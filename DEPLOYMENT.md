# Deployment

Production entry point: `index.html` at repository root.

GitHub Pages target URL:

`https://joaoccaldas.github.io/nasa-space-dashboard/`

Recommended Pages source: branch `main`, folder `/(root)`.

A mirrored `gh-pages` branch may also be used. The app is fully static and does not require a custom Actions workflow.
